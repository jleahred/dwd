reactivate events
    messages and actions with no mdl
    msg out and convert them to local messages when necessary
    simplify update on Main.elm

model common
    debug, log
    pass this model to all modules

Found
    view

websocket json

check material design unique ids